import React from 'react';

import { Container, StyledLoading } from './styles';

const Loader = () => {
  return (
    <Container>
      <StyledLoading />
    </Container>
  );
};

export default Loader;
